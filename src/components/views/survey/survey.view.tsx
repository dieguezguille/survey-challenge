import Container from '@mui/material/Container';

const SurveyView: React.FC = () => {
    return (
        <Container>
            <h2>Survey.</h2>
        </Container>
    );
};

export default SurveyView;

import { Box, Paper, styled } from '@mui/material';

export const CustomPill = styled(Paper)({
    padding: '5px',
    borderRadius: '50px',
    minWidth: '120px',
    marginLeft: '0.5em',
});

export const SurveyImageWrapper = styled(Box)({
    maxWidth: '30%',
    maxHeight: '30%',
});

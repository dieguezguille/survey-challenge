import Container from '@mui/material/Container';

const MainView: React.FC = () => {
    return (
        <Container>
            <h2>Welcome.</h2>
        </Container>
    );
};

export default MainView;

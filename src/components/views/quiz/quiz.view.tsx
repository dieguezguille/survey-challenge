import Container from '@mui/material/Container';

const QuizView: React.FC = () => {
    return (
        <Container>
            <h2>Quiz.</h2>
        </Container>
    );
};

export default QuizView;
